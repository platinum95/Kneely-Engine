#pragma once

#define _USE_MATH_DEFINES
#include <cmath>
#include <math.h>

class Trigonometry{
public:
	static float degreesToRadian(float degrees);
	static float radianToDegrees(float radians);
	static float ctan(float t);
};


